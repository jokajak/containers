---
app: tf-runner-bw
base: false
semantic_versioning: true
channels:
  - name: stable
    platforms: ["linux/amd64"]
    stable: true
    tests:
      enabled: false  # disabled because requires k8s env vars
      type: cli
